# human-readable-geo-position-in-photo

Python script that adds a human readable geo location to EXIF and to the photo file name.

Photos are loaded from `./Photos` folder. Filename must start with DSC.
